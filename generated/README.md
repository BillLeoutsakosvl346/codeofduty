# Generated ownership artifacts

`ownership-map.json` is rebuilt from the append-only ledger with `npm run ownership:rebuild`. It is safe to display in Code of Duty because it contains contribution identities and aggregate shares, not customer usage or payment records.
